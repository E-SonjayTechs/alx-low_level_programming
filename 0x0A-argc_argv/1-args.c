#include <stdio.h>
#include "main.h"

/**
 * main - entry point to the program
 * @argc: argument counter
 * @argv: argument vector arry and pointer
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void) argv;

	printf("%d", argc);
	printf("\n");

	return (0);
}
